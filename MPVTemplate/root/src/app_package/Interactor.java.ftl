package ${packageName};

import vn.iotech.base.viper.Interactor;

/**
 * The ${className} interactor
 */
class ${className}Interactor extends Interactor<${className}Contract.Presenter> 
	implements ${className}Contract.Interactor {

    ${className}Interactor(${className}Contract.Presenter presenter) {
        super(presenter);
    }
}
